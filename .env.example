# PS-SS14 Watchdog Docker Compose Configuration - Example File
# Copy this file to .env and modify the values for your deployment

# Application Settings
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://0.0.0.0:5000

# Port Configuration
INTERNAL_PORT=5000
EXTERNAL_PORT=5001
API_PORT=1212
POSTGRES_PORT=5432

# Volume Paths (Host Machine)
# This is the main directory containing all server instances
WATCHDOG_INSTANCES_PATH=/root/ss14-watchdog/instances
# Path to your appsettings.yml file on the host
WATCHDOG_APPSETTINGS_PATH=/root/ss14-watchdog/appsettings.yml

# Container Names
WATCHDOG_CONTAINER_NAME=PS-SS14-Watchdog

# Network Names
INTERNAL_NETWORK=ps-ss14
EXTERNAL_NETWORK=traefik_lan

# Traefik Configuration
TRAEFIK_DOMAIN=your-domain.example.com
TRAEFIK_CERT_RESOLVER=dns1
TRAEFIK_MIDDLEWARE=secure-headers
TRAEFIK_ENTRYPOINT=https

# ===========================================
# CONFIGURATION NOTICE
# ===========================================
# This file contains both Docker Compose variables and Traefik configuration.
# Application-specific settings should be configured in appsettings.yml
# at the path specified by WATCHDOG_APPSETTINGS_PATH

# Port Configuration:
# - INTERNAL_PORT (5000): Server-to-watchdog communication (HTTP)
# - EXTERNAL_PORT (5001): External proxy communication (HTTPS via Traefik)
# - API_PORT (1212): SS14 game server API communication
# - POSTGRES_PORT (5432): PostgreSQL database for inter-container communication

# ===========================================
# SECURITY NOTICE
# ===========================================
# DO NOT commit your actual .env file to GitHub!
# It may contain sensitive information like:
# - Domain names
# - SSL certificate configurations
# Always use this .env.example as a template.

# ===========================================
# CONFIGURATION FILE NOTICE
# ===========================================
# Your actual configuration should be in appsettings.yml
# This file contains sensitive data like:
# - API tokens
# - Discord webhook URLs
# - Database connection strings
# Make sure to keep your appsettings.yml secure!
