# SS14 Watchdog Docker Compose Configuration
# Copy this file to .env and configure your values

# Application Settings
ASPNETCORE_ENVIRONMENT=Production
# Use 0.0.0.0 to allow Traefik to connect from external networks
ASPNETCORE_URLS=http://0.0.0.0:8080

# Port Configuration
PORT=8080
API_PORT=1212

# Volume Paths (Host Machine)
# Update these paths to match your server setup
WATCHDOG_INSTANCES_PATH=/path/to/your/ss14-watchdog/instances
WATCHDOG_APPSETTINGS_PATH=/path/to/your/ss14-watchdog/appsettings.yml

# Container Names
WATCHDOG_CONTAINER_NAME=SS14-Watchdog
WATCHDOG_HOSTNAME=SS14-Watchdog

# Network Names
INTERNAL_NETWORK=ss14-internal
EXTERNAL_NETWORK=traefik_lan

# Traefik Configuration
# Update these domains to match your setup
TRAEFIK_DOMAIN=watchdog.yourdomain.com
GAME_DOMAIN=server.yourdomain.com
TRAEFIK_CERT_RESOLVER=letsencrypt
TRAEFIK_MIDDLEWARE=default@file
TRAEFIK_ENTRYPOINT=websecure
